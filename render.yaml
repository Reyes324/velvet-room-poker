services:
  - type: web
    name: velvet-room-poker
    runtime: node
    plan: free
    buildCommand: npm install --prefix server && cd client && npm install && npm run build
    startCommand: node server/index.js
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: "20"
