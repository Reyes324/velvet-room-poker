# 翡翠厅 — 项目规则

## SDD 优先工作流

SDD 文件位于 `openspec/changes/online-texas-holdem/`，包含 `design.md`（决策与场景）和 `tasks.md`（任务清单）。

**每次有设计决策、功能变更、功能取消时，必须按以下顺序执行：**

1. 先更新 `design.md` 和/或 `tasks.md`
2. 再写代码
3. commit 包含 SDD 更新

不等用户提醒，主动执行。

### 什么时候更新 SDD

| 变更类型 | 更新位置 |
|---|---|
| 取消/移除功能 | tasks.md 标 `~~cancelled~~` + 记原因；design.md 更新场景 |
| 设计决策变更 | design.md Decisions 节 |
| 新需求从对话浮现 | design.md Open Questions → 确认后加 tasks.md → 再写代码 |
| 任务完成 | tasks.md 勾选 `[x]` |
| 微调 UX（颜色/动画） | 不用更新，commit message 记录即可 |

## Git 规则

- 不经用户明确指令，不自动 push
- commit message 用英文，格式：`type: description`
- **push 前先查有没有人正在玩**：`curl https://velvet-room-poker.onrender.com/health`，看 `roomPlayers`（联机房间里真正连着的人数）和 `pvePlayersOnline`（真正连着的人机对战人数）是不是都是 0。push 会触发 Render 自动重新部署，服务器进程重启会清空内存里的牌局状态，正在玩的人会被直接踢出对局。任意一个 > 0 时先跟用户确认要不要等，不要闷头推（用户反馈，2026-08-16）。**不要用 `pveSessions`/`rooms` 判断"有没有人在玩"**——那两个是"存在多少个房间/多少个可恢复的对局"，跟"现在真的有人连着"是两码事：真实踩过一次，用户已经退出人机对战，`pveSessions` 却还显示 1（对局在 30 分钟窗口内被故意保留、方便断线重连），2026-08-17 加了 `roomPlayers`/`pvePlayersOnline` 这两个真实在线人数字段，改用这两个判断。

## 工作优先级

- **正确性 > 视觉打磨**：这是一个真的要给朋友联机打牌用的项目，不是 demo。除非用户主动提 UI/视觉需求，否则顺序永远是：正确性 bug → 测试覆盖 → 真实浏览器验证 → 最后才是视觉/动画打磨。
- **改动要结构化，不要打补丁**：收到改动需求（尤其是一批反馈）时，先找共性根因/所属的状态机或概念，再动手改，不要见一个症状加一个 if/一处判断。改完之后如果发现某处是"哪里疼医哪里"式的补丁（比如冗余判断、同一个常量在多处各存一份、状态被从类外部直接塞进对象属性而不是通过类自己的方法管理），要顺手理清楚，不要留着。
- **判断"两个改动是否会互相影响"这类问题时，不接受纯手算/读代码就下结论**——要么跑真实端到端测试，要么用 Playwright 之类工具实测（比如量真实渲染出来的 bounding box），再下结论。这条项目里已经因为"手算 CSS 觉得没问题，实测发现真的重叠"踩过坑。
- 判断一项任务是否"做完了"，不能只看 `tasks.md` 的勾选状态——要读代码、跑测试、真实跑一遍浏览器确认，checklist 可能没跟上实际进度。

## 反馈自动评估框架（2026-08-12 定稿）

处理 GitHub `feedback` 标签的 issue（无论是定时任务自动评估，还是跟用户对话时讨论）时，用下面三部分框架给结论，不是简单判断"合理/不合理"就完事：

1. **需求合理性**——这个想解决的问题本身站不站得住脚，是不是真问题。
2. **当前阶段的必要性**——项目现在处于"只有少量几个玩家"的朋友局阶段，很多在大规模场景下合理的功能，现阶段未必有必要。合理 ≠ 现在就该做。
3. **成本、收益、风险**：
   - 成本：不只是要不要花钱，还包括开发成本、后续维护成本。
   - 风险：会不会有合规问题、会不会跟其他已有产品设计/决策打架。

**额外的守门问题——产品复杂度**：新功能会不会让产品分支变多、变臃肿？这是独立于上面三条之外必须单独过一遍的问题——一个"合理、当前也有必要、成本收益都划算"的功能，如果会让 PRD/交互逻辑长出一堆新分支，也要慎重，尤其是用户没有提供充分背景/动机的情况下。

给结论时按这四点分别过一遍，不要只写"design/accepted，留给真人决策"这种笼统的话糊弄过去——每一点都要有具体判断，即使判断是"证据不足、暂不评估"。

**"是不是真问题"不用卡在"算不算 bug"这个标签上**（2026-08-28 补充）——影响到用户实际体验的问题（哪怕暂时定位不到根因、复现步骤不完整），也按真问题对待，主动去查、去搜，不要因为"技术上还不确定是什么原因"就当成信息不足直接晾着等对方补充。`bug`/`design` 标签只用来区分处理路径（代码修复 vs 产品方向决策），不用来判断这问题值不值得认真对待。

**自己判断不了的，评论里直说要讨论，不要硬凑结论**——遇到需要产品方向/优先级拍板、自己没把握给出四点评估的反馈，评论直接写类似"这个我们内部讨论一下再答复你，近期会跟进"，把决策权交回来，不要为了显得"处理完了"硬编一个未必站得住的结论。

**评论用大白话，不讲技术术语**——面向的是普通玩家，不是开发者。评论里不出现"AudioSession"、"MediaSession"、"根因"这类术语，用"占用/播放/系统声音"这种日常说法说清楚是什么问题、有没有解决、还需不需要用户配合。

**重新处理已经回复过的 issue，要留痕**——即使这条 issue 之前已经有过一次自动评论，只要现在又重新拿出来处理（讨论后改判断、或者真去修了），也要么新加一条评论说明最新结论，要么把旧评论编辑覆盖成新结论，不能让 issue 里留着一条已经过期、跟实际处理结果对不上的旧结论。

**"先做后可回退"（2026-08-28，门槛待定）**——对于影响体验又符合"自动修复的边界"四个条件的 bug，可以考虑先直接修复+commit（可用 git revert 撤销）再让用户验证效果，而不是只留评论等人拍板；具体门槛还没最终定，之前谈到的方向是：只对 bug 类开放，产品/视觉/方向类决策不适用；先做要不要直接 push 到 main（会立刻上线影响真实玩家）还是先开 PR 等确认再合，也还没拍板。

## 部署

- 线上地址：`https://velvet-room-poker.onrender.com`（Render 免费档，GitHub push 后自动重新部署）。免费档闲置会休眠，首次访问要等 30-60 秒冷启动。
- Railway 试用已过期未续费，不再使用。
- GitHub 仓库：`Reyes324/velvet-room-poker`。换电脑 `git clone` 这个仓库即可拿到完整代码——但 Claude 的记忆偏好文件是存在本机 `~/.claude/` 目录下的本地文件，不会跟着 git 走，换设备默认读不到（这也是为什么这份文件里要记这些内容）。
